# Abliteration — reserved

Status: **not started** on this Pro ARVQ checkpoint. No refusal or cybersecurity score is claimed for `MiMo-V2.6-Pro-ARVQ`.

This page is the slot for a later edit. It stays empty of numbers until a Pro tree is actually measured.

Constraints for that work, when it happens:

- Do not treat a MiMo-V2.6-Flash abliteration score as a result for this Pro checkpoint.
- Do not rewrite Jarrelscy's checkpoint in place. An abliterated tree gets its own directory and its own hash.
- The MTP tensors and the `dflash/` draft stay byte-identical unless a separate note says they were edited.
- The serve stays at GPU memory fraction **0.85**.

The serving recipe in the README does not load an abliteration overlay.
